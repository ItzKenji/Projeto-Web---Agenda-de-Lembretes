import { EfeitoSombraDirective } from './efeito-sombra.directive';

describe('EfeitoSombraDirective', () => {
  it('should create an instance', () => {
    const directive = new EfeitoSombraDirective();
    expect(directive).toBeTruthy();
  });
});
